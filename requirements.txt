# Runtime dependencies, pinned to exact versions.
# Transitive versions are resolved by pip; add a lock file (pip-tools / uv) if builds must be byte-reproducible.
Django==5.2.17
djangorestframework==3.18.1
django-filter==26.1
drf-spectacular==0.30.0
psycopg[binary]==3.3.6
requests==2.34.2
tenacity==9.1.4
django-environ==0.14.0
whitenoise==6.12.0
gunicorn==26.2.0
groq==1.7.0
